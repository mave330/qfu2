{
  "name": "qfu-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:api": "python -m uvicorn backend.server:app --host 0.0.0.0 --port 8000 --reload",
    "dev:web": "npm --prefix frontend run web",
    "build:web": "npm --prefix frontend run build:web",
    "start": "python -m uvicorn backend.server:app --host 0.0.0.0 --port 8000"
  }
}
